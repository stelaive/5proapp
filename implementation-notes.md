# Implementation Notes

지시에 명시되지 않은 판단이 필요했던 지점의 결정 로그.
원칙: **지시에 없으면 보수적인 쪽을 택하고 진행**, 대신 여기에 남긴다.

각 항목은 `무엇을 / 왜 / 되돌리려면` 순으로 기록한다.
"보수적"의 기준은 아래 순서를 따른다.

1. 기존 동작·기존 URL·기존 데이터를 깨지 않는 쪽
2. 삭제보다 보존, 자동 생성보다 명시
3. 되돌리기 쉬운 쪽 (한 곳만 고치면 원복되는 구조)
4. 사실이 확인되지 않은 문구는 쓰지 않는 쪽

---

## 2026-09-07 — 도메인 교체 (`xn--5-w30fr74e.com` → `www.5prosky.com`)

**확정된 전제 (사장님 확인받음):** canonical 호스트는 `https://www.5prosky.com` (apex 아님).
아래는 그 외에 지시 없이 내가 판단한 지점들.

### D-01. 이 파일의 위치를 프로젝트 루트로 정함

- **무엇을:** `implementation-notes.md`를 `docs/`가 아닌 저장소 루트에 생성.
- **왜:** 사장님이 경로 없이 파일명만 지정했으므로 문자 그대로 읽는 쪽이 보수적. `docs/`는 이미 기획·SEO 문서가 모인 곳이라 성격이 다른 작업 로그를 섞으면 찾기 어려워짐.
- **되돌리려면:** `git mv implementation-notes.md docs/` — 이 파일을 참조하는 코드는 없음.

### D-02. SEO 계획서의 옛 도메인 언급 1곳을 일부러 남김

- **무엇을:** [docs/SEO-AEO-GEO-PLAN.md](docs/SEO-AEO-GEO-PLAN.md) L43의 `5프로.com` / `xn--5-w30fr74e.com` 문자열을 교체하지 않음. 저장소 전체에서 유일하게 남은 옛 도메인 참조.
- **왜:** 해당 줄은 "**정규 도메인 표기 혼용**" 이라는 *과거에 있었던 문제*를 기록한 갭 분석 이력이다. 도메인명을 새 것으로 바꾸면 "www.5prosky.com과 www.5prosky.com이 혼용됨"이라는 뜻 없는 문장이 되어 기록 자체가 파괴된다. 기록 보존이 보수적.
- **되돌리려면:** 그 줄을 통째로 삭제하거나 "(구 도메인 시절 이슈, 2026-09-07 해소)" 주석을 덧붙이면 됨.
- **🔄 갱신 (2026-09-07, 사장님 지시로 번복):** 한글 도메인이 만료되어 회수 불가임이 확정됨. "아예 제거" 방침에 따라 이 판단을 뒤집고 옛 도메인 문자열을 전부 제거함. → **D-10** 참조.

### D-03. 도메인 문자열뿐 아니라 이를 설명하는 주석까지 수정

- **무엇을:** 단순 URL 치환 범위를 넘어 아래 두 곳의 설명 문구도 함께 고침.
  - [src/app/layout.tsx:35](src/app/layout.tsx#L35) `// 정규 URL (punycode 도메인으로 일원화)` → `(www.5prosky.com 으로 일원화)`
  - [public/robots.txt:19](public/robots.txt#L19) `# 사이트맵 위치 (정규 도메인 punycode 하나로 통일)` → `(정규 도메인 www.5prosky.com 하나로 통일)`
- **왜:** 새 도메인은 punycode가 아니므로 주석을 그대로 두면 코드와 설명이 어긋난다. 나중에 이 주석만 보고 "punycode로 되돌려야 하나" 오해할 여지가 있어, 거짓 설명을 남기는 쪽이 오히려 위험하다고 판단.
- **되돌리려면:** 주석 문구는 동작에 영향 없음. 취향에 맞게 자유롭게 재작성 가능.

### D-04. PRD의 한글 도메인 병기를 삭제

- **무엇을:** [docs/PRD.md:23](docs/PRD.md#L23) `| 도메인 | https://xn--5-w30fr74e.com (한글: 5프로.com) |` → `| 도메인 | https://www.5prosky.com |`. 괄호 안 한글 병기를 되살리지 않음.
- **왜:** `5prosky.com`은 순수 ASCII라 punycode 대응 한글 표기가 존재하지 않는다. 없는 별칭을 지어내지 않는 쪽이 보수적.
- **되돌리려면:** 한글 브랜드 도메인을 추가로 취득하면 그때 병기 복원.

### D-05. `.cursor/rules/sky.mdc`(AI 코딩 규칙 파일)도 수정 대상에 포함

- **무엇을:** [.cursor/rules/sky.mdc:18](.cursor/rules/sky.mdc#L18)의 `도메인: https://5프로.com`을 새 도메인으로 교체. `src/`·`public/` 밖이라 요청 범위가 모호했음.
- **왜:** 이 파일은 AI 에이전트가 읽고 코드를 생성하는 규칙 파일이다. 옛 도메인을 남겨두면 앞으로 생성되는 코드에 만료 도메인이 다시 주입된다. **오염원을 남기지 않는 쪽**이 보수적이라 판단.
- **되돌리려면:** 해당 줄 원복. 단 위 재오염 위험을 감수해야 함.

### D-06. 검색엔진 소유확인 파일은 건드리지 않음

- **무엇을:** [public/naver97498dbfce1b00edf82dc12eb340f09c.html](public/naver97498dbfce1b00edf82dc12eb340f09c.html), [public/DaumWebMasterTool.html](public/DaumWebMasterTool.html), [public/daum_webmaster_verification.txt](public/daum_webmaster_verification.txt), [public/robots.txt](public/robots.txt) 1~2행의 Daum 인증 해시 — 모두 원본 유지.
- **왜:** 검색엔진이 발급한 토큰이라 내용을 임의로 바꾸면 소유확인이 깨진다. 새 도메인 인증은 각 웹마스터도구에서 **새로 발급**받아야 하는 별개 절차. 코드에서 손댈 성질이 아님.
- **후속 조치 필요:** 네이버 서치어드바이저·다음 웹마스터도구에 `www.5prosky.com` 속성을 신규 등록하고 사이트맵 재제출.
- **🔄 갱신 (2026-09-07):** 네이버 인증 파일은 사장님이 커밋 `849b102`에서 이미 교체 완료(구 `naver97498...html` 삭제 → 신 `naverb43d9...html` 추가). 다음(Daum) 인증 파일은 미교체 상태로 남음 → **D-12** 참조.

### D-07. apex→www 301 리다이렉트를 설정 파일에 추가하지 않음

- **무엇을:** [vercel.json](vercel.json) / [netlify.toml](netlify.toml)에 리다이렉트 규칙을 넣지 않음.
- **왜:** 두 호스팅 모두 **대시보드 도메인 설정**에서 처리하는 것이 정석이고, 설정 파일에 규칙을 중복으로 넣으면 리다이렉트 체인이나 루프가 생길 수 있다. 어느 호스팅에 실제 배포 중인지도 미확인(설정 파일이 둘 다 존재). 인프라 영역에 코드로 개입하지 않는 쪽이 보수적.
- **후속 조치 필요:** 호스팅 대시보드에서 apex → www 301 설정.
- **🔄 갱신 (2026-09-07):** 구 도메인(`5프로.com`) → 신 도메인 301은 **불가능 확정**. 도메인이 만료되어 DNS 통제권이 없으므로 리다이렉트를 걸 방법이 없다. 기존 백링크·검색 색인은 자연 소멸을 기다리는 수밖에 없고, 대신 외부에 노출된 공식 링크를 직접 정정하는 경로로 전환 → **D-13** 참조.

### D-08. 도메인 상수를 공통 모듈로 통합하지 않음

- **무엇을:** 현재 도메인 문자열이 [layout.tsx](src/app/layout.tsx), [lib/metadata.ts](src/lib/metadata.ts), [lib/cityMetadata.ts](src/lib/cityMetadata.ts), [components/location/CityLayout.tsx](src/components/location/CityLayout.tsx), [components/seo/LocationJsonLd.tsx](src/components/seo/LocationJsonLd.tsx), [sitemap.ts](src/app/sitemap.ts), [rss.xml/route.ts](src/app/rss.xml/route.ts) 등 **7개 파일에 각각 하드코딩**돼 있다. 단일 상수로 합치자는 리팩터링을 하지 않음.
- **왜:** 요청은 "도메인 변경 점검"이지 구조 개선이 아니었다. 파일 7개를 건드리는 리팩터링은 범위를 임의로 넓히는 것이고, 지역 페이지 metadata에 회귀가 나면 SEO에 직접 타격이라 위험 대비 이득이 낮다고 판단.
- **남은 부채:** 다음에 도메인이 또 바뀌면 7곳을 다시 고쳐야 한다. 원한다면 `src/lib/siteConfig.ts` 하나로 모으는 작업을 별도 건으로 진행 가능. (환경변수 `NEXT_PUBLIC_SITE_URL` 도입도 같이 검토 — 현재 이 프로젝트는 `process.env` 사용처가 0건이고 `.env` 파일도 없음)

### D-09. 검증은 타입체크까지만, 프로덕션 빌드는 실행하지 않음

- **무엇을:** `npx tsc --noEmit`만 실행(통과). `npm run build`는 돌리지 않음.
- **왜:** 변경이 전부 문자열 리터럴 치환이라 타입체크로 충분하고, 전체 빌드는 시간이 오래 걸리며 `.next/` 산출물을 갈아치운다. 작업 흔적을 최소화하는 쪽이 보수적.
- **남은 리스크:** 낮음. 다만 배포 전 한 번은 `npm run build`로 metadataBase 경고가 사라졌는지 확인 권장.

---

## 2026-09-07 (2차) — 구 도메인 완전 폐기

**사장님 지시:** "5프로.com 한글도메인은 만료되어 우리가 건드릴 수 없으니 아예 제거한다는 생각으로 제거하고, 5prosky.com에 집중."

이로써 **D-02(옛 도메인 언급 보존)가 공식 번복**됨. 아래는 그 지시를 이행하며 새로 판단한 지점들.

### D-10. 갭 분석 행을 삭제하지 않고 "해소" 상태로 재작성

- **무엇을:** [docs/SEO-AEO-GEO-PLAN.md](docs/SEO-AEO-GEO-PLAN.md) G2 행에서 죽은 도메인명은 지웠지만 행 자체는 남기고 내용을 갱신함.
  - 전: `**정규 도메인 표기 혼용** — sitemap은 5프로.com(비인코딩), canonical은 xn--...(punycode) … | **P0** |`
  - 후: `**정규 도메인 통일** — 2026-09-07 해소. 전 URL을 www.5prosky.com으로 일원화(구 도메인은 만료·회수 불가로 완전 폐기) … | **해소** |`
- **왜:** 지시의 목적은 "죽은 도메인 문자열 제거"이지 "갭 분석표에 구멍 내기"가 아니다. G1~G10 번호로 본문 다른 곳에서 참조되는 구조라 행을 통째로 지우면 번호가 어긋나 문서가 깨진다. 문자열 제거 + 이력 보존을 동시에 만족시키는 쪽을 택함.
- **되돌리려면:** 해당 행 삭제. 단 G 번호 참조 정합성 확인 필요.

### D-11. 이 로그 파일 안의 옛 도메인 언급은 예외로 유지

- **무엇을:** `implementation-notes.md` 본문에는 `xn--5-w30fr74e.com` / `5프로.com` 문자열이 그대로 남아 있음. 저장소에서 유일한 예외.
- **왜:** 이 파일은 **"무엇에서 무엇으로 옮겼는가"를 기록하는 마이그레이션 로그**다. 출발점 도메인명을 지우면 "D-04. PRD의 한글 도메인 병기를 삭제" 같은 항목이 무엇을 지웠다는 건지 알 수 없어져 로그의 존재 이유가 사라진다. 사장님 지시의 대상은 *사이트가 외부에 노출하는 도메인*이지 내부 작업 기록이 아니라고 해석.
- **되돌리려면:** 이 파일 전체 삭제 또는 도메인명을 "(구 도메인)"으로 일괄 치환. 다만 후자는 D-04 같은 항목의 구체성이 사라짐.
- **참고:** 이 파일은 검색엔진에 노출되지 않는다(`public/` 밖, 빌드 산출물 아님). 색인 관점 리스크 없음.

### D-12. 다음(Daum) 웹마스터 인증 파일은 삭제하지 않고 유지

- **무엇을:** [public/DaumWebMasterTool.html](public/DaumWebMasterTool.html), [public/daum_webmaster_verification.txt](public/daum_webmaster_verification.txt), 그리고 [public/robots.txt](public/robots.txt) 1~2행의 `DaumWebMasterTool:` 해시를 원본 유지. (네이버 쪽은 사장님이 이미 교체 완료)
- **왜:** 구 도메인 시절 발급된 토큰이라 기능적으로는 죽은 값일 가능성이 높지만, ① 내가 재발급할 수 없는 값이라 **삭제가 비가역**이고 ② 남겨둬도 109바이트 정적 파일이 서빙될 뿐 SEO·보안상 해가 없다. 지시의 핵심은 "만료 도메인 URL 제거"인데 이 파일들에는 도메인 문자열이 없다. 비가역 삭제보다 무해한 잔존을 택하는 쪽이 보수적.
- **후속 조치 필요:** 다음 웹마스터도구에서 `www.5prosky.com` 신규 인증 → 새 파일 받으면 그때 구 파일 3개(robots.txt 상단 2행 포함) 정리. 네이버처럼 교체하시면 알려주세요.

### D-13. 구 도메인 301 계획을 폐기하고 "외부 링크 직접 정정"으로 전환

- **무엇을:** 이전 답변에서 권했던 "구 도메인 재등록 후 301로 링크 자산 이전"을 **철회**. [docs/new-md/seo최적화.md](docs/new-md/seo최적화.md) P2 항목의 두 갈래("이전 도메인 관리 가능 → 301 / 관리 불가 → 외부 공식 링크부터 정정") 중 **후자로 확정**.
- **왜:** 도메인 만료로 DNS 통제권이 없어 리다이렉트 자체가 물리적으로 불가능. 만료 도메인 재등록은 제3자 선점·프리미엄 가격·도메인 스쿼팅 리스크가 있어 사장님이 이미 "건드릴 수 없다"고 판단하신 사안. 되살리려 시도하지 않는 쪽이 보수적.
- **실무 영향:** 구 도메인으로 쌓인 백링크·검색 색인은 **회수 불가, 자연 소멸 대기**. 대신 우리가 통제 가능한 외부 노출 지점을 직접 고쳐야 함:
  - 네이버 플레이스 / 구글 비즈니스 프로필의 웹사이트 URL
  - 구글 플레이 · 앱스토어 앱 등록정보의 웹사이트 URL
  - 블로그·카페·명함·차량 랩핑·견적서 양식 등 오프라인 포함
  - 기존에 발행한 SNS 게시물 중 고정글·프로필 링크

### D-14. 체크리스트 항목을 삭제 대신 유효한 점검으로 대체

- **무엇을:** [docs/SEO-AEO-GEO-PLAN.md](docs/SEO-AEO-GEO-PLAN.md) 8절 `- [ ] 404/리다이렉트 점검 (한글 도메인 ↔ punycode)` → `- [ ] apex(5prosky.com) → www 301 리다이렉트 설정 및 404 점검 ※호스팅 대시보드 작업`. `- [ ] 전 URL punycode 정규화` 항목은 `- [x] 전 URL www.5prosky.com 정규화`로 완료 처리.
- **왜:** 한글↔punycode 점검은 무의미해졌지만 **"호스트 정규화 점검"이라는 과제 자체는 여전히 유효**(apex vs www). 항목을 지우면 아직 안 끝난 인프라 작업이 체크리스트에서 사라져 누락될 위험이 있어, 대상만 갈아끼움.
- **되돌리려면:** 해당 줄 삭제. 단 apex→www 설정이 끝난 뒤에 지울 것.

### D-15. 코드 주석의 "punycode" 표현 2곳 추가 정리

- **무엇을:** 1차 작업에서 놓쳤던 [src/app/layout.tsx:222](src/app/layout.tsx#L222), [src/app/sitemap.ts:5](src/app/sitemap.ts#L5)의 `punycode` 언급을 `www.5prosky.com`으로 교체.
- **왜:** 도메인 문자열이 아니라 **개념어만** 들어 있어 1차 grep(`xn--` / `5프로.com` 패턴)에 걸리지 않았다. D-03과 동일 논리 — 코드와 어긋나는 설명은 남기지 않음.
- **검증:** `npx tsc --noEmit` 통과. `src` `public` `docs` `.cursor` 전 범위에서 옛 도메인·punycode 흔적 0건 확인.

---

## 2026-09-07 (3차) — 메타데이터 정비 + 소식 페이지 신설

**사장님 지시:** "1. 데스크립션 2. 타이틀 3. 키워드 수정해야할것같고, 그리고 소식페이지 추가해야할것같아"

### D-16. 타이틀 브랜드명 2중 출력 — 자식 title에서 브랜드를 빼는 방식으로 수정

- **발견한 결함(추정 아님, 빌드 산출물로 확인):** 루트 [layout.tsx](src/app/layout.tsx)의 `title.template`이 `%s | 5프로돌려주는스카이차`인데, 모든 하위 layout이 `title`에 브랜드명을 **또** 적고 있었다. 실제 렌더링 결과:
  - `스카이차 가격표 | 5프로돌려주는스카이차 | 5프로돌려주는스카이차`
  - `왜 5프로돌려주는스카이차인가? | 5프로돌려주는스카이차 | 5프로돌려주는스카이차` (사실상 3회)
  - 지역 페이지 전체 동일 (`안양스카이차 | 5프로…(고소작업차) | 5프로…`)
- **선택지:** ⓐ 자식 title을 `{ absolute: '...' }`로 바꿔 템플릿을 무력화 ⓑ 자식 title에서 브랜드 접미사를 제거하고 템플릿이 1회 붙이게 함.
- **무엇을:** **ⓑ 선택.**
- **왜:** ⓐ는 기존 문구를 글자 그대로 보존한다는 점에서 더 보수적으로 보이지만, **템플릿을 죽은 설정으로 만들어 같은 실수가 재발**한다(앞으로 추가되는 페이지가 또 브랜드를 붙임). ⓑ는 템플릿을 설계 의도대로 되살려 재발을 구조적으로 막는다. 렌더링 결과 문자열도 기존 의도와 동일해진다. 루트 `SITE_INFO`에 경고 주석도 함께 남김.
- **주의:** `openGraph.title` / `twitter.title`은 **템플릿이 적용되지 않는 영역**이라 브랜드명을 그대로 유지했다. 여기까지 브랜드를 빼면 카카오톡·페이스북 공유 카드에서 브랜드가 사라진다.
- **검증:** `npm run build` 후 `.next/server/app/*.html`의 `<title>` 직접 확인. 전 페이지 브랜드 1회로 정상화.

### D-17. 지역 페이지는 중복 제거만, 문구 재작성은 하지 않음

- **무엇을:** anyang·gunpo·suwon·gangnam·seocho·[region] 6개 layout에서 브랜드 중복만 걷어내고, 지역별 문구·설명·키워드는 손대지 않음. 나머지 25개 자치구 페이지는 공용 [cityMetadata.ts](src/lib/cityMetadata.ts) 경유라 영향 없음.
- **왜:** 지시는 메인 메타데이터 3종에 대한 것이고, 지역 페이지 문구는 31개에 걸친 별도 콘텐츠 작업이다. [docs/new-md/지역검색_최적화.md](docs/new-md/지역검색_최적화.md)가 "지역명만 바꾼 페이지 대량 생성 금지"를 경고하고 있어, 지역 문구는 **실제 배차 실적 데이터를 보고 정해야 할 사안**이지 내가 일괄 치환할 성질이 아님.
- **부수 정리:** gangnam·seocho의 `… | ${data.phone} | 5% 페이백해주는 스카이차`에서 마지막 마디만 제거. 브랜드명이 이미 "5프로돌려주는"이라 의미가 중복되고, 템플릿 적용 후 4마디가 되어 검색결과에서 잘리기 때문. 전화번호는 사장님 의도로 보고 유지.

### D-18. 검증 불가한 표현만 골라 제거, 운영 근거가 있는 표현은 유지

[docs/new-md/](docs/new-md/) 문서들이 반복 경고하는 "운영 사실 확인 전에는 쓰지 말 것" 원칙을 적용하되, **근거 유무로 갈랐다.**

| 표현 | 처리 | 근거 |
|---|---|---|
| `최저가` (홈) | **삭제** | 검증 불가한 최상급. 표시광고법상 위험도 있음 |
| `바가지 없는` (가격표) | **삭제** | 경쟁사 폄하 뉘앙스. 디자인 문서가 명시적으로 지양 권고 |
| `즉시 돌려받기` (가격표) | **완화** → `현금 환급` | [비용_환급페이지.md](docs/new-md/비용_환급페이지.md)가 "즉시 지급 여부 미확정" 상태를 정면으로 지적 |
| `통장에 돈이 계속 쌓인다` (친구초대) | **삭제** | 수익 보장으로 읽힐 수 있음 |
| `기존 스카이차 업체와 차별화된` (여긴뭐야) | **삭제** | 막연한 경쟁사 비교 |
| `24시간` | **유지** | [PRD.md](docs/PRD.md) "운영 시간 24시간" + [support 페이지 FAQ](src/app/support/page.tsx)가 "24시간 배차"를 명시 — 사장님이 공표한 운영 정책 |
| `전국(제주 제외)` | **유지** | PRD·FAQ·구조화 데이터 모두 일치 |

- **왜 이렇게 갈랐나:** "의심스러우면 전부 삭제"가 가장 안전해 보이지만, **사장님이 이미 공표한 운영 정책까지 내가 임의로 지우는 것은 월권**이다. 근거 문서가 있는 표현은 남기고, 근거 없는 최상급·비교·시점 약속만 제거하는 쪽을 택함.
- **되돌리려면:** 각 layout의 `description` 문자열 복원. 단 위 위험을 다시 안게 됨.

### D-19. `1톤~5톤` → `1톤~19톤` 오류 수정

- **무엇을:** 루트 layout description의 `1톤~5톤`을 `1톤~19톤`으로 정정.
- **왜:** [pricing/page.tsx](src/app/pricing/page.tsx) L22의 실제 요금표가 `1톤~19톤`이고, 구조화 데이터·RSS·미사용 metadata.ts도 전부 19톤이었다. **홈 description만 5톤으로 어긋나 있어** 실제 보유 장비 범위를 축소 표기하던 상태. 판단이랄 것 없는 명백한 오류 수정.

### D-20. 키워드는 대폭 축소하되 제거하지는 않음

- **무엇을:** 홈 48개 → 13개. 하위 5개 페이지는 **전부 가격표용 키워드(55개)를 복붙**해 쓰고 있던 것을 페이지별 주제어 5~11개로 교체.
- **왜:** `keywords` 메타 태그는 구글이 공식적으로 무시하고 네이버도 순위 요소로 쓰지 않는다. **삭제해도 순위 손실은 없지만**, 사장님이 직접 넣으신 자산이라 통째로 없애는 것은 과한 개입으로 판단 → 주제를 대표하는 핵심어만 남기는 절충.
- **실질적 이득은 축소 자체보다 "페이지별 분리":** 모든 페이지가 "스카이차가격"을 1순위로 주장하던 상태(카니발라이제이션)를 해소했다. 홈 키워드에 섞여 있던 `욕실리모델링·유품정리·CCTV·철거·닥트공사` 등 일거리장터 업종어도 제거 — 스카이차와 무관해 주제를 흐렸음.
- **되돌리려면:** git에서 이전 문자열 복원.

### D-21. 미사용 모듈 [lib/metadata.ts](src/lib/metadata.ts)는 삭제하지 않고 값만 정합화

- **무엇을:** 이 파일은 **어느 페이지에서도 import 되지 않는 죽은 코드**(grep 0건). 삭제하지 않고 ① 미사용 경고 주석 추가 ② description·keywords를 새 기준에 맞춤 ③ `fullTitle`이 브랜드를 붙이던 로직을 제거하고 og/twitter용 `socialTitle`을 분리.
- **왜:** 삭제가 깔끔하지만 요청 범위 밖이고 비가역이다. 반대로 낡은 값을 그대로 두면, 나중에 누가 이 함수를 되살렸을 때 **D-16의 브랜드 중복 버그와 폐기된 문구가 그대로 부활**한다. "지우지도 방치하지도 않고 안전한 상태로 재워둠"을 택함.
- **남은 부채:** 정말 안 쓸 거면 삭제, 쓸 거면 각 layout을 이 함수로 통일. 지금은 같은 정보가 두 곳에 존재.

### D-22. 소식 페이지: 상세 라우트 없는 목록형 + 서버 컴포넌트

- **무엇을:** [/news](src/app/news/page.tsx) 한 페이지에 전체 소식을 카드로 펼치는 구조. 항목별 상세 URL(`/news/[id]`)은 만들지 않고 `#id` 앵커만 부여. 데이터는 [src/lib/newsData.ts](src/lib/newsData.ts)로 분리.
- **왜 상세 라우트를 안 만들었나:** 지금 실을 소식이 1건이다. 상세 라우트를 파면 **내용 없는 얇은 페이지가 색인**되는데, [new-md 문서](docs/new-md/seo최적화.md)가 반복해서 경고하는 상황이다. 목록형은 소식이 쌓인 뒤 상세 라우트로 확장하기 쉽지만 그 반대는 어렵다.
- **왜 서버 컴포넌트인가:** [디자인 (1).md](docs/new-md/디자인%20(1).md)의 최우선 티켓 T01이 "본문이 스크롤 애니메이션 이후에만 보이는지 검증"이다. 소식은 **JS 없이도 HTML에 본문이 들어있어야** 하는 성격이라 `'use client'`·framer-motion을 의도적으로 배제.
- **검증:** 빌드된 `.next/server/app/news.html`에 본문 텍스트가 실제로 포함됨을 확인.

### D-23. 소식 내용은 사실 1건만, 나머지는 지어내지 않음

- **무엇을:** 등록 항목은 **도메인 변경 안내 1건**뿐. 샘플·더미 소식을 만들지 않았고, 항목이 0건일 때의 빈 상태 UI를 함께 구현.
- **왜:** [new-md 문서](docs/new-md/aeo_geo최적화.md) 전반이 "사실 없는 후기·실적·수치는 만들지 않음"을 못박고 있다. 도메인 변경은 **실제로 일어난 일이고 고객에게 실용적인 정보**(옛 즐겨찾기가 죽음)라 유일하게 근거가 확실했다.
- **데이터 파일에 경고 주석 삽입:** "확정되지 않은 정책·일정·수치는 올리지 않습니다."
- **사장님 확인 필요:** 소식 본문에 쓴 "이전 주소로는 더 이상 접속되지 않습니다"가 사실인지. 만료 도메인이 제3자에게 넘어가 다른 페이지가 뜨는 상황이면 문구를 바꿔야 함.

### D-24. RSS 피드에 소식 항목을 실제로 포함

- **무엇을:** [rss.xml/route.ts](src/app/rss.xml/route.ts)가 정적 페이지 6개만 나열하던 것에 `NEWS_ITEMS`를 앞쪽에 합류시킴.
- **왜:** RSS는 원래 "새 소식"을 구독시키는 규격인데 기존 피드는 **변하지 않는 페이지 목록**이라 사실상 무의미했다. 소식 페이지를 만든 이상 피드에 흘리는 것이 자연스러운 완성이라 판단. 다만 이는 지시에 없던 확장이므로 기록.
- **되돌리려면:** `[...newsEntries, ...pages]`를 `pages`로 되돌리면 됨.
- **참고:** sitemap의 `/news` `lastModified`는 최신 소식 게시일과 자동 동기화되도록 `LATEST_NEWS_DATE`를 연결. 손으로 날짜를 고칠 필요가 없다.

### D-25. 소식 페이지에 NewsArticle 구조화 데이터를 넣지 않음

- **무엇을:** JSON-LD 미적용.
- **왜:** `NewsArticle`은 언론 기사용 스키마라 공지사항에 붙이면 부적절하고, 구조화 데이터는 [new-md 문서](docs/new-md/seo최적화.md) 기준 "실제 정보와 일치, 검증 도구 통과"가 전제다. 항목 1건 상태에서 스키마부터 붙이는 건 순서가 뒤바뀜. 소식이 쌓이면 `ItemList` 또는 `BlogPosting` 검토.

### D-26. 발견했으나 고치지 않은 사실 불일치 (범위 밖)

- **무엇:** **시간 초과 추가금이 두 곳에서 다르다.**
  - [support/page.tsx](src/app/support/page.tsx) FAQ: "기준 시간을 초과하면 **시간당 15만원**"
  - [lib/regionData.ts:239](src/lib/regionData.ts#L239): "시간 초과(**+5만원/시간**)"
- **왜 안 고쳤나:** 어느 쪽이 실제 요금인지 나는 알 수 없다. 추측으로 한쪽에 맞추면 **잘못된 가격을 고객에게 고지**하게 되어 도메인 교체보다 훨씬 위험하다. 요금은 사장님만 확정할 수 있는 정보.
- **사장님 확인 필요:** 실제 초과 요금을 알려주시면 양쪽을 맞추겠습니다. [비용_환급페이지.md](docs/new-md/비용_환급페이지.md)가 "가장 중요한 신뢰 페이지"로 규정한 영역이라 우선순위 높음.

### D-27. 검증을 위해 프로덕션 빌드를 실행함 (D-09 방침 변경)

- **무엇을:** D-09에서 "빌드는 돌리지 않는다"고 했으나 이번엔 `npm run build`를 2회 실행.
- **왜:** 타이틀 중복은 **타입체크로는 절대 잡히지 않고**, 소스만 읽어서는 Next.js 템플릿 적용 여부를 단정할 수 없다. 결함을 추측으로 보고하지 않으려면 실제 렌더링 HTML 확인이 유일한 방법이었다. `.next/`는 gitignore 대상이라 작업 흔적도 남지 않음.

---

## 2026-09-07 (4차) — 외부 체크리스트 대입 점검 (수정 없음)

**사장님 지시:** "[SEO_AEO_GEO_체크리스트.md](docs/new-md/SEO_AEO_GEO_체크리스트.md) 확인해봐. 다른 업체 체크리스트인데 우리꺼에 대입해보고싶어."

### D-28. 점검만 하고 수정은 하지 않음 (범위 판단)

- **무엇을:** 체크리스트 3·4·5·6장을 우리 코드에 대입해 점검만 수행. 발견한 결함을 고치지 않고 보고만 함.
- **왜:** 지시가 "확인/대입"이지 "수정"이 아니었다. 특히 아래 canonical 건은 8개 페이지의 색인 정책을 바꾸는 작업이라 사장님 판단을 받고 진행하는 것이 맞다고 봄. 다만 결함이 묻히지 않도록 여기에 남김.
- **검증 방식:** 추측을 배제하기 위해 전부 빌드 산출물(`.next/server/app/*.html`)에서 직접 확인.

### F-01. 🔴 canonical이 8개 페이지에서 홈으로 몰려 있음 (미수정)

- **증상:** `pricing` `support` `whyhere` `reward` `marketplace` `locations/seoul` `locations/gyeonggi` `locations/incheon` 의 canonical이 전부 `https://www.5prosky.com`(홈).
- **원인:** 루트 [layout.tsx](src/app/layout.tsx)의 `alternates: { canonical: '/' }`를 자식 layout이 그대로 상속. 자기 canonical을 선언한 곳은 `news`(4차 작업에서 추가)와 개별 도시 페이지들뿐.
- **의미:** 검색엔진에 "가격표·고객센터는 홈의 중복이니 홈을 색인하라"고 지시하는 상태. 해당 페이지들이 색인에서 빠질 수 있음.
- **근거 문서 2곳이 동일 경고:** 외부 체크리스트 4.1-⑤ "canonical 일관성 부족 → 레이아웃 레벨에서 강제", [seo최적화.md](docs/new-md/seo최적화.md) P1 "지역 페이지들을 전부 홈으로 canonical 처리 금지".
- **수정안:** 각 layout에 `alternates: { canonical: '/경로' }` 추가(8곳). 또는 루트에서 `canonical: '/'`를 제거하고 페이지마다 필수 선언으로 강제.
- **주의:** 루트의 `canonical: '/'`를 그냥 지우면 홈의 canonical이 사라진다. 홈 전용 선언을 남긴 뒤 제거해야 함.

### F-02. 🟠 whyhere H1 0개 · 애니메이션 초기상태 opacity:0 다수 (미수정)

- **증상:** `whyhere` 페이지에 `<h1>`이 없음(체크리스트 4.1-④ 위반). 초기 HTML에서 `opacity:0`으로 렌더된 요소가 whyhere 37개, 홈 21개, pricing 3개.
- **의미:** [디자인 (1).md](docs/new-md/디자인%20(1).md)가 최우선 티켓 T01로 지목한 "중앙 공백·하단 검은 영역"의 유력한 원인. 본문 자체는 HTML에 있어 크롤러는 읽지만, **JS 실패·느린 연결·애니메이션 최소화 설정에서는 사용자에게 빈 화면**이 된다.
- **수정안:** 첫 화면 요소는 애니메이션 초기값을 `opacity:1`로 두거나 `prefers-reduced-motion` 대응 추가. whyhere에 H1 부여.

### F-03. 🟡 이미지 최적화가 설정으로 무력화됨 (미수정)

- **증상:** [next.config.js:11](next.config.js#L11) `unoptimized: true`. `next/image`를 22개 컴포넌트가 쓰는데 최적화가 꺼져 있어 바로 위 `formats: ['image/webp','image/avif']` 설정이 무의미.
- **의미:** 체크리스트 2장 "이미지 최적화 모듈 미설치" 항목에 해당. LCP 직접 악화 요인.
- **수정 전 확인 필요:** `unoptimized: true`는 보통 정적 호스팅 제약 때문에 넣는다. 배포처(Vercel/Netlify) 확정 후 꺼야 이미지가 깨지지 않음.

### F-04. 기존 문서의 낡은 기록 정정

- [SEO-AEO-GEO-PLAN.md](docs/SEO-AEO-GEO-PLAN.md) G4 "FAQPage 구조화 데이터 없음"은 **더 이상 사실이 아님**. 빌드 결과 `support` 20문항, 홈 5문항의 FAQPage JSON-LD가 이미 적용돼 있다.
- 다만 [aeo_geo최적화.md](docs/new-md/aeo_geo최적화.md)가 "구글 FAQ 확장 결과는 2026-05-07 종료"라고 했으므로, **유지하되 리치결과 노출은 기대하지 않는다**가 현재 기준.

### F-05. D-26(요금 불일치)의 심각도 상향

- 외부 체크리스트 6.1이 명시: *"자사 사이트 내에서도 페이지마다 숫자가 다르면 어느 것도 확신 있게 인용하지 않음"*.
- 즉 시간 초과 요금 **15만원(FAQ) vs 5만원(regionData)** 불일치는 단순 오타가 아니라 **AI 답변엔진이 우리 요금 정보 전체를 인용 대상에서 배제하는 사유**가 된다. D-26에서 "확인 필요"로 둔 항목이지만 우선순위를 올려야 함.

---

## 2026-09-07 (5차) — 체크리스트 지적사항 일괄 수정

**사장님 지시:** "일단 다 진행해" (F-01~F-05 및 제안 순서 1~5 전부)

### D-29. F-01 canonical — 루트 상속을 없애지 않고 페이지별 선언을 추가

- **무엇을:** `pricing` `support` `whyhere` `reward` `marketplace` 5개 layout에 `alternates.canonical` 추가. `[region]` 동적 layout에는 `` `/locations/${regionSlug}` ``를 두 반환 경로(정상·fallback) 모두에 추가 → `seoul` `gyeonggi` `incheon` 3개 해결. 합계 8개.
- **선택하지 않은 방법:** 루트 layout의 `canonical: '/'`를 제거하고 전 페이지 필수 선언으로 강제하는 방식.
- **왜:** 루트에서 제거하면 홈의 canonical이 사라지고, 앞으로 canonical 선언을 빠뜨린 페이지는 **아예 canonical이 없는 상태**가 된다. 지금 구조는 빠뜨려도 최소한 홈을 가리키므로 덜 위험하다. 각 layout에 "없으면 홈의 중복으로 취급된다"는 경고 주석을 남겨 재발을 막는 쪽을 택함.
- **검증:** 클린 빌드 후 11개 페이지 canonical 전부 자기 URL을 가리키는 것 확인.

### D-30. [region] 허브 3개가 동일 메타데이터를 공유하던 문제 (F-01 작업 중 추가 발견)

- **발견:** `getRegionData('seoul')`이 null을 반환한다. 광역 허브 슬러그는 도시 데이터에 없기 때문. 그 결과 `seoul` `gyeonggi` `incheon` 3개 페이지가 **fallback 메타데이터를 그대로 공유**해 title·description이 완전히 동일했다. 서로 중복 콘텐츠 신호가 된다.
- **덤으로 드러난 것:** 3차 작업(D-16)에서 이 파일의 title은 고쳤다고 생각했으나, fallback 쪽 title은 들여쓰기가 6칸이라 2칸을 전제한 정규식이 빗나가 **브랜드 중복이 그대로 남아 있었다**. 빌드 HTML을 다시 확인하지 않았다면 놓쳤을 부분.
- **무엇을:** `REGION_LABELS` 맵(seoul→서울, gyeonggi→경기, incheon→인천)을 추가해 허브별 고유 title·description 생성. 본문 경로의 `` `... | 5프로` `` 접미사도 제거.
- **왜 이 방식인가:** 지역명은 슬러그에서 기계적으로 나오는 **사실**이라 지어낸 정보가 아니다. 반대로 "배차 가능 시간", "보유 장비" 같은 건 허브별로 다를 수 있어 넣지 않았다.
- **교훈(작업 방식):** 일괄 정규식 치환 후에는 소스가 아니라 **빌드 산출물로 검증**해야 한다. D-16에서 소스 기준으로만 확인했다면 이 누락을 그대로 넘겼을 것.

### D-31. F-02 애니메이션 — 코드를 대량 수정하지 않고 폴백 2겹으로 처리

- **문제:** 초기 HTML에서 `opacity:0`으로 렌더되는 요소가 whyhere 37개·홈 21개. JS가 안 돌면 본문이 영영 안 보인다. whyhere 히어로는 등장 지연이 최대 **3초**였다.
- **선택하지 않은 방법:** 모든 `motion.*`의 `initial`을 제거하거나 `useReducedMotion`을 전 컴포넌트에 도입.
- **무엇을:** ⓐ 루트 layout에 `<noscript>` 스타일 추가 — JS 미실행 시 `opacity:0` 요소를 강제로 보이게 함. ⓑ `@media (prefers-reduced-motion: reduce)` 블록 추가 — 모션 최소화 사용자에게 즉시 표시. ⓒ whyhere 히어로 지연만 3초 → 0.7초로 단축.
- **왜:** 애니메이션 자체는 사장님이 의도한 연출이라 걷어내는 건 월권이다. **정상 환경에서는 연출을 그대로 두고, 실패 환경에서만 개입**하는 2겹 폴백이 가장 적은 변경으로 T01을 해소한다. 지연 3초는 연출이라기보다 이탈 요인이라 판단해 단축.
- **한계(솔직히):** `opacity:0` 요소 수 자체는 줄지 않았다(41개). 폴백은 JS 실패·모션 최소화 상황만 커버하며, **JS가 느리게 로드되는 정상 브라우저에서는 여전히 최대 0.7초 빈 화면**이다. 실기기 확인이 필요하다.

### D-32. F-02 whyhere H1 — 눈썹 문구가 아니라 실제 헤드라인을 H1으로

- **무엇을:** 히어로의 `motion.h3`("여긴뭐야?")를 `motion.p`로 낮추고, 실제 헤드라인을 담고 있던 `<div>`("아니, 스카이차 쓰는데 5%를 바로 돌려준다고?…")를 `<h1>`으로 승격.
- **왜:** "여긴뭐야?"를 H1으로 만드는 게 더 간단했지만 검색어로서 가치가 없다. H1은 **그 페이지가 무엇에 대한 것인지 말하는 문장**이어야 하고, 체크리스트 5.1도 "개체 → 카테고리 → 차별점" 순의 자기완결적 문장을 요구한다. 또한 H1보다 앞선 H3는 헤딩 계층 위반이라 함께 정리.
- **검증:** 빌드 HTML에서 `whyhere` H1 = 1개.

### D-33. F-03 이미지 최적화 — 배포처를 못 물어봤지만 켜기로 판단

- **무엇을:** [next.config.js](next.config.js)의 `unoptimized: true` 제거.
- **왜 위험을 감수했나:** `unoptimized`는 보통 정적 export(`output: 'export'`) 제약 때문에 넣는다. 그런데 ① `next.config.js`에 `output: 'export'`가 없고 ② 빌드 결과에 `ƒ (Dynamic)` 서버 라우트(`/rss.xml`)가 존재한다. **즉 이 프로젝트는 정적 export가 아니라 서버 배포**이므로 이미지 최적화가 동작하는 환경이다. `remotePatterns`도 `hostname: '**'`이라 외부 이미지가 막힐 일이 없다.
- **되돌리려면:** `images` 안에 `unoptimized: true,` 한 줄 복구.
- **⚠️ 배포 후 눈으로 확인 필요:** 빌드는 통과했지만 이미지가 실제로 잘 나오는지는 배포 환경에서만 확인 가능하다. 깨지면 위 한 줄 복구로 즉시 원복.

### D-34. F-04 robots.txt AI 크롤러 — 아무것도 새로 차단하지 않음

- **무엇을:** 검색용 봇(`OAI-SearchBot` `ChatGPT-User` `Claude-SearchBot` `Claude-User` `PerplexityBot`)과 학습용 봇(`GPTBot` `ClaudeBot` `Google-Extended` `CCBot`)을 **구분해서 명시**하되, 전부 `Allow: /`로 둠.
- **왜 학습용을 막지 않았나:** 학습 데이터 이용을 허용할지는 **사업·저작권 판단**이지 기술 판단이 아니다. 현재 상태(`User-agent: *  Allow: /`)가 이미 전면 허용이므로, 내가 임의로 Disallow를 넣으면 **지시 없이 정책을 바꾸는 것**이 된다. 기능적으로는 변화가 없고 정책만 명문화한 상태.
- **바꾸는 법:** 학습용 3~4개 블록의 `Allow: /`를 `Disallow: /`로 바꾸면 됨. robots.txt에 그 방법을 주석으로 적어뒀다. 학습용을 막아도 검색 노출에는 영향이 없다(체크리스트 5.5).

### D-35. F-05 요금 불일치 — "다 진행해"에도 불구하고 수정하지 않음

- **무엇을:** 시간 초과 추가금 **15만원(support FAQ) vs 5만원(regionData)** 를 그대로 둠.
- **왜 지시를 따르지 않았나:** 실제 요금은 내가 알 수 없는 정보다. 한쪽으로 맞추면 **고객에게 잘못된 가격을 고지**하게 되고, 그건 되돌리기 어려운 실제 피해다. 추측으로 고치는 것보다 불일치를 남기고 보고하는 쪽이 안전하다고 판단.
- **추가로 파악한 단서:** [regionData.ts:27](src/lib/regionData.ts#L27)의 타입 정의가 `additionalHour?: number; // 추가 시간당 (1톤/3.5톤만)`이고 실제 값은 `50000`(2곳). 즉 **5만원은 1톤·3.5톤 소형 장비 한정 단가**일 가능성이 있다. 그렇다면 FAQ의 15만원은 대형 장비 기준이거나, 둘 중 하나가 낡은 값이다.
- **사장님 확인 필요 (이번 작업 유일한 미해결):** ① 초과 요금이 장비별로 다른가 ② 다르다면 각각 얼마인가 ③ 같다면 15만원과 5만원 중 무엇이 맞는가. 알려주시면 FAQ와 regionData 양쪽을 맞추고, 장비별로 다르면 FAQ 문구에 조건을 명시하겠습니다.

---

## 2026-09-07 (6차) — 요금 불일치 해소 (D-35 후속)

**사장님 답변:** `/pricing` 의 "장비별 이용요금" 표가 기준. (스크린샷 제공)

### D-36. 원본은 [priceData.ts](src/lib/priceData.ts) — 요금이 3곳에 흩어져 있었음

사장님 답변으로 기준이 확정되어 전수 대조한 결과, 요금 정보가 **서로 동기화되지 않는 3개 소스**에 존재했다.

| 소스 | 성격 | 상태 |
|---|---|---|
| [priceData.ts](src/lib/priceData.ts) `defaultPriceData` | `/pricing` 표의 원본. 스크린샷과 일치 | ✅ 기준 |
| [regionData.ts](src/lib/regionData.ts) `GANGNAM_DATA.pricing` | 지역 페이지 표. 별도 하드코딩 | ❌ 3곳 오류 |
| [support/page.tsx](src/app/support/page.tsx) FAQ 문자열 | 사람이 쓴 안내문 | ❌ 조건 누락 |

### D-37. 발견한 오류 4건과 수정 내용

| # | 위치 | 수정 전 | 수정 후 | 근거 |
|---|---|---|---|---|
| 1 | 강남 1톤/3.5톤 `additionalHour` | 50,000 | **150,000** | 공식표 "추가(시간당) 150,000원" |
| 2 | 강남 5톤 `additionalHour` | 50,000 | **200,000** | 공식표 "추가(시간당) 200,000원" |
| 3 | 강남 5톤 `oneHour` | 350,000 | **300,000** | 공식표 "1시간 300,000원" |
| 4 | 강남 5톤 `thirtyMin` | 없음(화면에 `—`) | **300,000** | 공식표 "30분 300,000원" |

- **③④를 함께 고친 이유:** 사장님 답변은 추가요금 건에 대한 것이었지만, 대조 과정에서 5톤 1시간·30분도 어긋난 것이 드러났다. 강남의 **나머지 6개 값은 공식표와 완전히 일치**(200,000 / 250,000 / 350,000 / 550,000 / 450,000 / 650,000)하는데 이 둘만 다르다. 즉 강남만의 특별 요금이 아니라 **입력 오류**로 보는 것이 타당했다. 그대로 두면 `/pricing`과 `/locations/gangnam`이 서로 다른 가격을 고지하게 된다.
- **위험도 비교:** 추가요금 5만원 표기는 실제 15만~20만원 대비 **고객에게 유리하게 잘못 안내**되던 값이라 현장 분쟁 소지가 컸다. 반대로 5톤 1시간 35만원은 공식표보다 **5만원 비싸게** 안내되던 값이다. 양쪽 다 실제 거래에 영향을 주는 오류.
- **검증:** 클린 빌드 후 강남 페이지 표가 공식표와 완전 일치함을 확인.
  `1톤/3.5톤 200,000 / 250,000 / 350,000 / 550,000` · `5톤 300,000 / 300,000 / 450,000 / 650,000`

### D-38. FAQ는 숫자를 바꾸지 않고 "조건"을 추가

- **문제:** FAQ가 *"기준 시간을 초과하면 **시간당 15만원**"*이라고 **조건 없이 단정**하고 있었다. 15만원은 1톤~3.5톤에만 맞고 5톤은 20만원, 8톤 이상은 협의다.
- **무엇을:** 장비별로 나눠 서술. `1톤~3.5톤 15만원 / 5톤(45m) 20만원 / 8톤 이상 협의`.
- **왜 이 방식인가:** 원래 숫자(15만원)가 틀린 게 아니라 **적용 범위가 빠진 것**이었다. 숫자를 갈아끼우는 게 아니라 조건을 붙이는 것이 사실에 맞는 수정.

### D-39. FAQ의 "대여 비용은 전국 동일" 표현 완화 (별건 발견)

- **발견:** [priceData.ts:25-27](src/lib/priceData.ts#L25)의 `suwonPriceData`는 추가요금이 **1톤~3.5톤 250,000 / 5톤 350,000**으로 기본값(150,000 / 200,000)과 다르다. 주석도 "실제 가격표 반영"이라 **의도적인 지역 차등**으로 보인다.
- **그런데** FAQ는 *"안내된 대여 비용은 전국 동일하게 적용됩니다"*라고 단언하고 있었다. 반나절·하루 기본요금은 전국 동일하지만 **추가요금은 그렇지 않다.**
- **무엇을:** "반나절·하루 기본 요금은 전국 동일하게 적용되며, 시간 초과 추가금은 일부 지역에서 기준이 다를 수 있어 예약 시 안내해 드립니다"로 수정.
- **왜 수원 금액을 FAQ에 쓰지 않았나:** `suwonPriceData`가 현재도 유효한지 확인되지 않았다. 검증 안 된 숫자를 FAQ로 퍼뜨리기보다 **범위를 열어두는 서술**이 안전하다고 판단.
- **사장님 확인 필요:** 수원 추가요금 차등이 지금도 맞는지. 맞다면 FAQ에 명시하는 편이 낫습니다.

### D-40. 죽은 코드 발견 — [locations/gangnam/components/](src/app/locations/gangnam/components/) 7개 파일

- **무엇:** `PriceTable.tsx` `FooterNotice.tsx` `HeroSection.tsx` `BenefitCards.tsx` `BookingProcess.tsx` `TonnageGuide.tsx` `TrustSection.tsx` — **어디서도 import 되지 않음.** 실제로 `gangnam/page.tsx`는 `CityPageTemplate` 하나만 쓴다.
- **왜 알아야 하나:** 이 중 `PriceTable.tsx`에는 화면에 없는 **"추가 (1시간)" 컬럼**이 있다. 즉 `additionalHour` 값은 현재 어느 화면에도 표시되지 않고, 지역 페이지의 추가요금 안내는 **`notice` 문자열에만** 존재한다. 이번에 `additionalHour`를 고친 것은 화면 표시가 아니라 **데이터 정합성**을 위한 것.
- **삭제하지 않은 이유:** 요청 범위 밖이고, 되살려 쓸 계획이 있을 수 있다. [D-21](implementation-notes.md)의 `lib/metadata.ts`와 같은 판단.
- **남은 부채:** `regionData.pricing`이 `priceData.ts`를 참조하지 않고 값을 복제하는 구조라 **언제든 다시 어긋난다.** 지역 페이지가 `defaultPriceData`를 기본값으로 쓰고 예외 지역만 덮어쓰는 구조로 바꾸면 이 부류의 오류가 구조적으로 사라진다. 별건으로 진행 가능.

---

## 2026-09-07 (7차) — 전국 동일 요금 확정, D-39 정정

**사장님 답변:** "수원도 같아. 전국 다 같은 가격."

### D-41. D-39에서 내가 넣은 "지역별 상이 가능" 문구는 틀렸으므로 되돌림

- **무엇을:** 6차에서 FAQ를 *"기본 요금은 전국 동일하며, 시간 초과 추가금은 일부 지역에서 기준이 다를 수 있어…"*로 고쳤던 것을 **"안내된 대여 비용은 지역과 관계없이 전국 동일하게 적용됩니다"**로 되돌림. 초과요금 문장에도 "추가금 기준도 전국 동일하게 적용됩니다"를 명시.
- **왜 틀렸나:** `suwonPriceData`의 값(추가 250,000/350,000)을 **의도적인 지역 차등으로 해석**했다. 주석에 "실제 가격표 반영"이라 적혀 있어 근거가 있다고 봤지만, 실제로는 **그 데이터 자체가 틀린 값**이었다. 코드 주석을 사실의 근거로 삼은 것이 오판.
- **다행이었던 점:** 그때 수원 금액(25만/35만)을 FAQ 본문에 그대로 옮겨 적지 않고 "다를 수 있다"고만 열어뒀다. 숫자를 퍼뜨렸다면 틀린 요금이 고객 안내문에 박힐 뻔했다. **검증 안 된 수치를 문구로 확정하지 않는다**는 원칙이 실제로 사고를 막았다.

### D-42. 지역별 요금 배열을 없애고 전부 defaultPriceData를 가리키게 함

- **무엇을:** [priceData.ts](src/lib/priceData.ts)의 `gunpoPriceData` `suwonPriceData`를 각각의 하드코딩 배열에서 `defaultPriceData` 별칭으로 교체. (`anyangPriceData`는 이미 그 형태였음)
- **왜 값만 고치지 않았나:** 요금이 전국 동일하다면 **지역별 배열의 존재 자체가 불일치의 원인**이다. 값만 맞춰 두면 다음에 요금이 바뀔 때 또 한쪽만 고쳐진다. 실제로 이번 사고가 정확히 그 형태였다. 별칭으로 바꾸면 구조적으로 어긋날 수 없다.
- **부수 효과(의도한 것):** 수원 표가 4행 → 7행(19톤·3톤 굴절·5톤 굴절 추가), 30분·1시간 컬럼도 노출된다. 전국 동일 요금이므로 **공식표 전체를 보여주는 것이 맞다.**
- **삭제하지 않은 이유:** `export` 이름은 유지했다. 죽은 컴포넌트들이 아직 이 이름을 import하고 있어 지우면 컴파일이 깨진다.
- **재발 방지 주석 삽입:** *"지역마다 값을 복제해 두면 한쪽만 고쳐져 /pricing 과 지역 페이지가 서로 다른 가격을 고지하게 됩니다. 실제로 그런 사고가 있었습니다."*

### D-43. 죽은 코드 범위 확정 — locations/{anyang,gangnam,suwon}/components 17개 파일

- **확인:** 28개 도시 페이지 전부가 `page.tsx`에서 `regionData` + `CityPageTemplate` **둘만** import한다. 지역별 `components/` 디렉터리(anyang 6 · gangnam 7 · suwon 4 = 17개 파일)는 어디서도 쓰이지 않는다.
- **의미:** `suwonPriceData`의 잘못된 값도 실제 화면에는 나오지 않았다(수원 페이지는 `CityPageTemplate` 경유). 즉 **고객에게 잘못 노출된 것은 아니었지만**, 되살릴 때 터질 지뢰였다.
- **삭제하지 않음:** 요청 범위 밖. [D-21](implementation-notes.md)·[D-40](implementation-notes.md)과 동일 판단.

### D-44. 최종 검증 — 전 지역 전수 대조

빌드 산출물에서 10개 지역 페이지(수원·안양·군포·강남·서초·송파·마포·노원·용산·중구)의 요금표를 공식표와 대조.

```
1톤/3.5톤   200,000  250,000  350,000  550,000
5톤         300,000  300,000  450,000  650,000
```

**10개 전부 일치, 불일치 0건.** 나머지 18개 도시도 `...GANGNAM_DATA` 스프레드라 동일한 값을 상속한다.
